# =============================================================================
# dsfr-data - Variables d'environnement
# =============================================================================
# Copier ce fichier en .env et adapter les valeurs a votre environnement.
#
#   cp .env.example .env
#
# Convention :
#   [REQUISE] : doit etre definie pour un build/deploy de production. Le check
#               `npm run validate:build-env` echoue sinon (cf. issue #168 PR-3).
#               Les scripts deploy.sh et deploy-server.sh les generent
#               automatiquement a partir de APP_DOMAIN si absentes.
#   [optionnelle] : defaut sain, surcharge possible.
#   [serveur]  : utile uniquement en mode "serveur" (Express + MariaDB),
#                pas en mode statique.
# =============================================================================

# ---------------------------------------------------------------------------
# Domaine de production [REQUISE pour Traefik / deploy]
# ---------------------------------------------------------------------------
# Nom de domaine sur lequel l'application est deployee.
# Utilise par Traefik (docker-compose.yml) et les scripts de deploiement.
# Sert aussi de valeur par defaut pour VITE_PROXY_URL et APP_URL via les
# scripts deploy.sh / deploy-server.sh.
APP_DOMAIN=chartsbuilder.matge.com

# ---------------------------------------------------------------------------
# URL du proxy CORS [REQUISE au build] (build-time)
# ---------------------------------------------------------------------------
# URL de base du proxy CORS pour les appels API en production.
# Propagee au build Docker via docker-compose `build.args` (cf. issue #168).
#
# Depuis PR-3 de l'epic #168, cette variable est REQUISE au build de
# production : `npm run build:all` echoue si elle manque. Les scripts
# deploy.sh et deploy-server.sh la generent automatiquement a partir de
# APP_DOMAIN, donc le deploiement de reference fonctionne sans config
# manuelle.
#
# Pour `npm run build:all` en local sans .env (ex: tests, dev), bypass
# explicite via `DSFR_DATA_DEV_BUILD=1 npm run build:all`.
#
# En dev (Vite dev server), cette variable est ignoree (le proxy Vite gere).
# VITE_PROXY_URL=https://chartsbuilder.matge.com

# ---------------------------------------------------------------------------
# Domaine d'embed (build-time, optionnel) -- separation app vs widgets
# ---------------------------------------------------------------------------
# URL de base inlinee dans le code genere par les builders (widgets destines
# a etre colles sur un site tiers). Utile pour self-heberger l'app sur un
# domaine interne tout en generant des widgets qui pointent vers un domaine
# public stable (ex. CDN, instance partagee). Cf. issue #180.
#
# Si non defini, fallback en cascade sur VITE_PROXY_URL (= cas du deploiement
# de reference : runtime et embed sur le meme domaine).
#
# Concerne : code genere par apps/builder, apps/builder-ia, apps/builder-carto.
# Ne concerne PAS les widgets Grist (apps/grist-widgets), le monitoring, ni
# les appels runtime de l'app -- ils restent sur VITE_PROXY_URL.
# VITE_PROXY_URL_EMBED=https://cdn.example.com

# ---------------------------------------------------------------------------
# URL de collecte du beacon de telemetrie (build-time, optionnel)
# ---------------------------------------------------------------------------
# URL utilisee par le bundle lib (packages/core/dist/dsfr-data.*.js) pour
# envoyer le beacon de telemetrie. Le bundle etant distribue sur npm/CDN et
# charge par des sites tiers, l'URL doit pointer vers un domaine qui heberge
# la collecte (typiquement = domaine d'embed). Cf. issue #180.
#
# Si non defini, fallback en cascade sur VITE_PROXY_URL_EMBED puis
# VITE_PROXY_URL (= cas du deploiement de reference).
#
# Utile pour : pointer les beacons vers un domaine d'analytics distinct
# (ex. analytics.example.com) different du domaine de l'app et de l'embed.
# VITE_BEACON_URL=https://analytics.example.com

# ---------------------------------------------------------------------------
# URL de la librairie JS dans le code genere (build-time)
# ---------------------------------------------------------------------------
# URL de base pour les bundles dsfr-data dans le code HTML genere (builders).
# Propagee au build Docker via docker-compose `build.args` (cf. issue #168).
# Valeurs speciales :
#   "jsdelivr" (defaut si non defini) → https://cdn.jsdelivr.net/npm/dsfr-data@0/dist
#   "unpkg"                           → https://unpkg.com/dsfr-data@0/dist
#   "self"                            → ${VITE_PROXY_URL}/dist (self-hosted)
# Sinon, une URL custom (ex: https://cdn.example.com/dist).
# VITE_LIB_URL=unpkg

# ---------------------------------------------------------------------------
# Proxy HTTP sortant (build-time + runtime)
# ---------------------------------------------------------------------------
# Permet `docker compose build` puis `docker compose up` derriere un proxy
# d'entreprise. Deux portees :
#   - Build-time : `npm ci` + `apk add` dans le stage builder des Dockerfile
#     (cf. issue #168 PR-2).
#   - Runtime   : les services Node a l'interieur du conteneur honorent ces
#     memes variables via undici.EnvHttpProxyAgent (depuis PR-4). Concerne :
#     `scripts/ia-default-server.js` (proxy IA Albert) et le `mcp-server`
#     (telechargement de skills.json au demarrage). Sans variable, aucun
#     dispatcher proxy n'est installe : comportement strictement inchange.
#
# Conventions POSIX : majuscules et minuscules acceptees. NO_PROXY est une
# liste separee par virgules d'hostnames a contacter en direct (par ex.
# le proxy interne d'un VPS, ou les hostnames Docker internes comme
# `mariadb`, `mailserver`).
# HTTP_PROXY=http://corporate.proxy.example.com:8080
# HTTPS_PROXY=http://corporate.proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1,mariadb,mailserver,.example.com

# ---------------------------------------------------------------------------
# JWT Secret (mode serveur uniquement)
# ---------------------------------------------------------------------------
# Cle secrete pour la signature des tokens JWT (mode serveur avec auth).
# Generee automatiquement par deploy-server.sh si absente.
# JWT_SECRET=

# ---------------------------------------------------------------------------
# MariaDB (mode serveur uniquement)
# ---------------------------------------------------------------------------
# Connexion a la base MariaDB. Les mots de passe sont generes automatiquement
# par deploy-server.sh si absents.
# DB_HOST=mariadb
# DB_PORT=3306
# DB_NAME=dsfr_data
# DB_USER=dsfr_data
# DB_PASSWORD=
# DB_ROOT_PASSWORD=

# ---------------------------------------------------------------------------
# Chiffrement des cles API (mode serveur uniquement)
# ---------------------------------------------------------------------------
# Cle AES-256-GCM pour chiffrer les api_key_encrypted dans la table connections.
# 64 caracteres hexadecimaux (32 bytes). Generer avec : openssl rand -hex 32
# Generee automatiquement par deploy-server.sh si absente.
# ENCRYPTION_KEY=

# ---------------------------------------------------------------------------
# CSRF Secret (mode serveur uniquement)
# ---------------------------------------------------------------------------
# Cle HMAC pour la generation/validation des tokens CSRF (csrf-csrf v4).
# Generer avec : openssl rand -hex 32
# Si non defini, fallback sur ENCRYPTION_KEY (portees disjointes, AES vs HMAC).
# CSRF_SECRET=

# ---------------------------------------------------------------------------
# SMTP - Envoi d'emails [serveur]
# ---------------------------------------------------------------------------
# Serveur SMTP pour l'envoi d'emails (verification, bienvenue).
# Par defaut, utilise le conteneur docker-mailserver sur ecosystem-network.
# Pas d'authentification requise depuis le reseau Docker interne.
# SMTP_HOST=mailserver
# SMTP_PORT=25
# SMTP_FROM=noreply@ecosysteme.matge.com

# ---------------------------------------------------------------------------
# URL de l'app dans les emails [REQUISE en mode serveur] (runtime)
# ---------------------------------------------------------------------------
# URL absolue inseree dans les liens des emails (verification, reset password,
# bienvenue). Depuis PR-3 de l'epic #168, plus de fallback vers le domaine
# de reference : si manquante, l'envoi d'email throw a la premiere
# utilisation. Le script deploy-server.sh la genere automatiquement a partir
# de APP_DOMAIN pour preserver le deploiement de reference.
# APP_URL=https://chartsbuilder.matge.com

# ---------------------------------------------------------------------------
# Reverse proxy externe (mode serveur uniquement)
# ---------------------------------------------------------------------------
# Express doit faire confiance aux en-tetes X-Forwarded-* du proxy pour que
# req.ip, req.secure et les rate-limiters refletent le vrai client.
# Valeurs : un entier (nombre de hops proxy, ex. 1 ou 2), 'true'/'false', ou
# une valeur Express ('loopback', sous-reseau). Defaut : 'loopback' (cas de
# reference ou nginx parle a l'API via 127.0.0.1 dans le meme hote).
# Un proxy sur une AUTRE machine doit definir le nombre de hops.
# TRUST_PROXY=1

# Vu que la verification email exige un SMTP joignable + APP_URL, un deploiement
# sans serveur mail (ex. derriere un proxy tiers) ne peut pas activer les
# comptes. Mettre a 'false' pour auto-verifier les comptes a l'inscription et
# connecter l'utilisateur immediatement (pas d'email envoye). Defaut : true.
# REQUIRE_EMAIL_VERIFICATION=false

# CORS — necessaire seulement si l'app (page) et l'API sont sur des ORIGINES
# differentes (sous-domaines ou domaines distincts). En same-origin, inutile.
# CORS_ORIGIN : origines exactes autorisees, separees par des virgules.
# CORS_ORIGIN=https://app.example.com,https://autre.example.com
# CORS_ALLOW_SUBDOMAINS_OF : autorise tout sous-domaine d'un parent (CSV).
# Ex. 'actimage.net' autorise https://app.actimage.net, https://api.actimage.net…
# ATTENTION : autorise le CORS AVEC credentials. Ne mettez qu'un domaine que
# vous maitrisez ENTIEREMENT. JAMAIS un suffixe public partage (gouv.fr, fr…).
# CORS_ALLOW_SUBDOMAINS_OF=actimage.net

# ---------------------------------------------------------------------------
# Bootstrap admin (mode serveur uniquement, TEMPORAIRE)
# ---------------------------------------------------------------------------
# Si ces DEUX variables sont definies, un compte admin (email auto-verifie) est
# cree au demarrage s'il n'existe pas deja. Sert a debloquer un deploiement sans
# SMTP. A RETIRER apres connexion (sinon le compte est recree s'il est supprime),
# et changer le mot de passe ensuite. Le mot de passe doit faire >= 8 caracteres
# avec au moins une minuscule, une majuscule et un chiffre.
# SEED_ADMIN_EMAIL=admin@example.com
# SEED_ADMIN_PASSWORD=ChangeMe123

# ---------------------------------------------------------------------------
# Albert IA - Configuration serveur par defaut (Builder IA)
# ---------------------------------------------------------------------------
# Si ces variables sont definies, le Builder IA fonctionne sans que l'utilisateur
# ait besoin de configurer sa propre cle API. Le token reste cote serveur (jamais
# expose au client). L'utilisateur peut surcharger avec sa propre config.
#
# IMPORTANT : PAS de prefixe VITE_ pour eviter l'injection dans le bundle JS.
# Obtenir un token Albert : https://albert.api.etalab.gouv.fr
#
# IA_DEFAULT_API_URL=https://albert.api.etalab.gouv.fr/v1/chat/completions
# IA_DEFAULT_MODEL=albert-large
# IA_DEFAULT_TOKEN=
